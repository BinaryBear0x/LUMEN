<Window x:Class="HuePC.App.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LUMEN" Width="530" Height="580" WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" AllowsTransparency="True"
        Background="Transparent" Topmost="True" UseLayoutRounding="True">
    <Border Margin="12" CornerRadius="28" BorderThickness="1" BorderBrush="#526A64" Background="#111B21">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="8" Opacity="0.45" Color="#000000" />
        </Border.Effect>
        <Grid ClipToBounds="True">
            <Ellipse Width="350" Height="350" Margin="0,-96,-145,0" HorizontalAlignment="Right" VerticalAlignment="Top"
                     Fill="#132B582F" />
            <Ellipse Width="320" Height="320" Margin="-170,0,0,-150" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                     Fill="#102C5E41" />
            <Grid Margin="34,27,34,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <TextBlock Text="LUMEN" FontSize="46" FontWeight="Bold" Margin="0,10,0,0"
                               HorizontalAlignment="Center"
                               FontFamily="pack://application:,,,/Assets/Fonts/#Pirata One, Segoe UI"
                               Foreground="#E7F3E9" />
                </Grid>

                <Grid Grid.Row="1" Margin="0,12,0,6">
                    <Ellipse Width="275" Height="275" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#4B90C537" Offset="0" />
                                <GradientStop Color="#003B6D39" Offset="1" />
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.6" To="1"
                                                         Duration="0:0:2.5" AutoReverse="True" RepeatBehavior="Forever" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    <Ellipse Width="172" Height="15" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                             Margin="0,0,0,26" Fill="#5C77B638">
                        <Ellipse.Effect><BlurEffect Radius="19" /></Ellipse.Effect>
                    </Ellipse>
                    <Image Source="Assets/lumen-servo-skull-splash.png" Width="350" Height="430"
                           Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"
                           RenderOptions.BitmapScalingMode="NearestNeighbor">
                        <Image.RenderTransform><TranslateTransform /></Image.RenderTransform>
                        <Image.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                         From="-10" To="10" Duration="0:0:2.7" AutoReverse="True" RepeatBehavior="Forever">
                                            <DoubleAnimation.EasingFunction><SineEase EasingMode="EaseInOut" /></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Image.Triggers>
                    </Image>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
